# Compose Desktop blocks Homebrew JDK packaging by default. This keeps local
# package smoke tests runnable; release builds should use a production JDK.
compose.desktop.packaging.checkJdkVendor=false
